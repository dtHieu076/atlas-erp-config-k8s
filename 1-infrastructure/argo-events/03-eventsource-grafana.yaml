apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: grafana-webhook
  namespace: argo-events
spec:
  serviceAccountName: remediation-sa
  webhook:
    # Định nghĩa 1 endpoint tên là 'alert'
    alert: 
      port: "12000"
      endpoint: /grafana
      method: POST