apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-activity-deployment
  labels:
    app: user-activity-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-activity-api
  template:
    metadata:
      labels:
        app: user-activity-api
      annotations:
        backup.velero.io/backup-volumes: backup-storage
    spec:
      containers:
      - name: user-activity-container
        image: dthieudocker/user-activity-service:413a184c6c42e16dd971c4e8e145736189a9d09a
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: backup-storage
          mountPath: /app/backups
      volumes:
      - name: backup-storage
        persistentVolumeClaim:
          claimName: shared-backup-pvc